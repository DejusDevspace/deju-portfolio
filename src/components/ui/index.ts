/**
 * d3jusdevspace — UI Component barrel export.
 *
 * Base UI primitives (buttons, inputs, badges, skeletons, etc.)
 * live in this directory and are re-exported from here.
 */

export { default as LoadingSpinner } from "./loading-spinner";
export type { SpinnerSize } from "./loading-spinner";
